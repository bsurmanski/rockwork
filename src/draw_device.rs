extern crate gl;

use std::fmt;
use std::io::Read;
use std::io::Error;
use std::error;
use std::ffi::CString;
use gl::types::*;

pub struct GLContext {
}

impl GLContext {
    fn new(w: usize, h: usize) {
    }

    fn clear(&mut self) {
    }
}


